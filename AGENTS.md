# AGENTS.md

이 문서는 Sendway 리포에서 작업하는 에이전트(및 사람)를 위한 규칙입니다.

## 원칙 (우선순위 순)

1. **배포 유연성** — 클라우드와 온프레미스에서 동일하게 동작해야 한다. 특정 클라우드 벤더 전용 기능에 의존하지 않는다.
2. **얇은 책임 경계** — 발송만 책임진다. 수신자 신원, 구독 설정, 테넌시 같은 개념을 끌어들이지 않는다.
3. **통합 속도** — 위 두 원칙과 충돌하지 않는 선에서 소비 서비스의 통합 비용을 최소화한다.

원칙이 충돌하면 번호가 낮은 쪽이 이깁니다.

## 비목표

- 수신자 신원 정보 관리
- 구독 설정(수신 동의, 채널별 선호) 관리
- 대량 마케팅 캠페인성 발송
- 발송 이력을 보여주는 운영자용 대시보드

## 현재 품질 램프 레벨

목표: L1(사용 가능). 통과 기준은 `dev-docs/quality-ramp.md` 참고. 유예 항목을 임의로 L1 범위에 넣거나 빼지 말 것.

## 유예 목록 요약

- 다음 채널(SMS/슬랙·텔레그램) 착수 조건
- L2/L3 통과 기준
- 정확한 API 시그니처
- 큐 기술 선택

전체 목록과 상태: `dev-docs/open-questions.md`, `dev-docs/backlog.md`

## 문서 이원화 규약

- `docs/`: 현재형, 확정된 결론만. 커밋되고 공개됩니다. 코드와 불일치하면 버그로 취급합니다.
- `dev-docs/`: 과정·검토·수치·손절선·경쟁 분석. `.gitignore` 대상이며 공개하지 않습니다.
- 판정 질문: "6개월 뒤 이 문장이 틀려졌다면, 그건 고쳐야 할 버그인가 아니면 그냥 옛날 기록인가?" 버그면 `docs/`, 기록이면 `dev-docs/`.
- `docs/`에서 `dev-docs/`로 향하는 링크는 만들지 않는다 — 공개 저장소를 클론한 사람에게는 깨진 링크입니다.
- 승격(promotion)만이 `dev-docs → docs` 이동 경로입니다. 결정이 확정되고 코드에 반영되면, 결론만 `docs/`에 현재형으로 다시 씁니다(복사가 아니라 재작성). 승격 이력은 `dev-docs/promotion-log.md`에 남깁니다.

## 공개 산출물 언어 정책

이 리포는 공개 OSS입니다. `docs/`, README, 커밋 메시지, 코드 주석, 테스트 이름 등 공개 산출물에는 다음을 노출하지 않습니다:

- 사내 반복구현 방지라는 실제 개발 동기, 발견 경위 서술
- 특정 사내 프로젝트/리포 이름
- 목표 채택 수치, 손절선, 내부 일정
- 절대경로, 내부 호스트명, 개인명

실제 동기와 배경은 `dev-docs/`에만 남습니다. 애매하면 `dev-docs/`로 보냅니다.
