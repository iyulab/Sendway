# 개념

상태: 이 문서는 현재 구현을 기술한다

## 채널(Channel)

메시지가 전달되는 매체. 현재는 이메일 하나만 있습니다.

## 어댑터(Adapter)

채널별 발송 프로토콜(현재는 SMTP)을 연결하는 구성요소. 채널이 하나뿐이라 아직 채널 간 공통 인터페이스로 일반화되어 있지 않습니다.

## SDK 모드 / Docker 서비스 모드

Sendway를 시작하는 두 경로로 계획되어 있으나, 현재는 **Docker 서비스** 모드만 존재합니다. 독립 실행형 서비스를 띄워 HTTP API로 호출합니다.
